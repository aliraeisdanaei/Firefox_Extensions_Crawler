This is a simple reposetory to collect some data about all Firefox extensions into a csv file. 

Make sure all of the requirements are installed by running
pip install -r requirements.txt

To collect all data, make sure all cap values are off. 
